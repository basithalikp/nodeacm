<!-- Event Card Full Component (for events page grid) -->
<div class="card event-card-full event-gallery-trigger" 
     data-event-id="<%= event.id %>"
     data-event-title="<%= event.title %>"
     data-event-date="<%= event.date %>"
     data-event-photos='<%= JSON.stringify(event.photos || [event.image]) %>'
     role="button"
     tabindex="0"
     aria-label="View gallery for <%= event.title %>">
    <div class="card-img-container">
        <img src="<%= event.image %>" alt="<%= event.title %>" class="card-img" loading="lazy">
    </div>
    <div class="card-body">
        <h3 class="card-title"><%= event.title %></h3>
        <p class="card-date"><%= event.date %></p>
        <p class="card-text"><%= event.description %></p>
        <button class="view-gallery-btn-permanent">
            <i class="fas fa-images"></i> View Details
        </button>
        <% if (event.registerUrl) { %>
        <button class="register-now-btn" data-register-url="<%= event.registerUrl %>" data-event-title="<%= event.title %>" onclick="event.stopPropagation(); openRegisterModal(this)">
            <i class="fas fa-user-plus"></i> Register Now
        </button>
        <% } %>
    </div>
</div>
